export function DemoDataNotice() {
  return (
    <div className="mb-4 rounded-md border border-arcane/30 bg-arcane/10 px-4 py-3 text-sm text-parchment">
      <strong className="text-arcane">Preview data.</strong> Covenant hasn&apos;t launched
      competitive seasons yet — these standings are seeded example data, not real match results.
    </div>
  );
}
